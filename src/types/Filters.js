/* src/types/Filters.js */
/* REMOVED: All TypeScript interfaces (City, Neighborhood, Cuisine) */
// This file now contains no executable code.
// It previously defined TypeScript types. You can keep it for documentation
// or remove it if types are managed elsewhere or not needed in JS.

/*
Expected shape for City:
{
    id: number;
    name: string;
}

Expected shape for Neighborhood:
{
    id: number;
    name: string;
    city_id: number;
    city_name?: string; // Optional
}

Expected shape for Cuisine (Hashtag with category='cuisine'):
{
    id: number;
    name: string;
    category?: string; // Should be 'cuisine'
}
*/