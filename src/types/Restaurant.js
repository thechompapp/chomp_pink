/* src/types/Restaurant.js */
/* REMOVED: All TypeScript interfaces (Restaurant, RestaurantDetail, FeaturedList, SimilarPlace) */
// This file now contains no executable code.
// If these structures were only used for type checking, this file might be removable
// or repurposed for JavaScript data validation functions if needed.

// Example (if you needed JS validation):
/*
function validateRestaurant(data) {
  if (!data || typeof data.id !== 'number' || typeof data.name !== 'string') {
    console.error("Invalid restaurant data:", data);
    return false;
  }
  // Add more checks based on your requirements
  return true;
}
*/
// export { validateRestaurant }; // Example export