/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {
      // your custom theme config stays here
    },
  },
  plugins: [
    // your existing plugins stay here
  ],
};
